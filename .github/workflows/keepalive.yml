name: Keep Alive

on:
  schedule:
    - cron: "0 14 * * 0,3"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render health endpoint
        run: curl -sf https://fintrack-ai-7sv8.onrender.com/health
