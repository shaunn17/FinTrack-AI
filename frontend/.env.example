# Dev: leave unset OR use http://localhost:8000 — both use the Vite proxy (same origin /api).
# Only set a custom URL if the API is not on this machine (e.g. http://192.168.1.5:8000).
# VITE_API_BASE_URL=
